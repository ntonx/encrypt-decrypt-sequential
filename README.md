# encrypt-decrypt-sequential
Execute on console:
locate over src/ directory and execute this command:

java main/Lab01 DES 64 -c password /d/dataset/


where /d/dataset is the path which contains files 
